#create_event_modal.modal.hide.fade
    .modal-header Create New Event
        button.close(data-dismiss='modal') x
    .modal-content
        form#new_event_form(method="post")
            fieldset(name="event-creation")
                input.event_name(required="required", type="text", title="Event name", name="event-name", placeholder="event name")
                input.event-submit(type="submit", value="Create Event!")